<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[supervisor进程管理使用教程]]></title>
    <url>%2Fpost%2F86c9ee8.html</url>
    <content type="text"><![CDATA[使用 supervisor 管理进程Supervisor (http://supervisord.org) 是一个用 Python 写的进程管理工具，可以很方便的用来启动、重启、关闭进程（不仅仅是 Python 进程）。除了对单个进程的控制，还可以同时启动、关闭多个进程，比如很不幸的服务器出问题导致所有应用程序都被杀死，此时可以用 supervisor 同时启动所有应用程序而不是一个一个地敲命令启动。 安装Supervisor 可以运行在 Linux、Mac OS X 上。如前所述，supervisor 是 Python 编写的，所以安装起来也很方便，可以直接用 pip:1sudo pip install supervisor supervisord 配置Supervisor 相当强大，提供了很丰富的功能，不过我们可能只需要用到其中一小部分。安装完成之后，可以编写配置文件，来满足自己的需求。为了方便，我们把配置分成两部分：supervisord（supervisor 是一个 C/S 模型的程序，这是 server 端，对应的有 client 端：supervisorctl）和应用程序（即我们要管理的程序）。 首先来看 supervisord 的配置文件。安装完 supervisor 之后，可以运行echo_supervisord_conf 命令输出默认的配置项，也可以重定向到一个配置文件里：1echo_supervisord_conf &gt; /etc/supervisord.conf 去除里面大部分注释和“不相关”的部分，我们可以先看这些配置:12345678910111213141516171819202122232425262728293031323334[unix_http_server]file=/tmp/supervisor.sock ; UNIX socket 文件，supervisorctl 会使用;chmod=0700 ; socket 文件的 mode，默认是 0700;chown=nobody:nogroup ; socket 文件的 owner，格式： uid:gid;[inet_http_server] ; HTTP 服务器，提供 web 管理界面;port=127.0.0.1:9001 ; Web 管理后台运行的 IP 和端口，如果开放到公网，需要注意安全性;username=user ; 登录管理后台的用户名;password=123 ; 登录管理后台的密码[supervisord]logfile=/tmp/supervisord.log ; 日志文件，默认是 $CWD/supervisord.loglogfile_maxbytes=50MB ; 日志文件大小，超出会 rotate，默认 50MBlogfile_backups=10 ; 日志文件保留备份数量默认 10loglevel=info ; 日志级别，默认 info，其它: debug,warn,tracepidfile=/tmp/supervisord.pid ; pid 文件nodaemon=false ; 是否在前台启动，默认是 false，即以 daemon 的方式启动minfds=1024 ; 可以打开的文件描述符的最小值，默认 1024minprocs=200 ; 可以打开的进程数的最小值，默认 200; the below section must remain in the config file for RPC; (supervisorctl/web interface) to work, additional interfaces may be; added by defining them in separate rpcinterface: sections[rpcinterface:supervisor]supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface[supervisorctl]serverurl=unix:///tmp/supervisor.sock ; 通过 UNIX socket 连接 supervisord，路径与 unix_http_server 部分的 file 一致;serverurl=http://127.0.0.1:9001 ; 通过 HTTP 的方式连接 supervisord; 包含其他的配置文件[include]files = relative/directory/*.ini ; 可以是 *.conf 或 *.ini我们把上面这部分配置保存到 /etc/supervisord.conf（或其他任意有权限访问的文件），然后启动 supervisord（通过 -c 选项指定配置文件路径，如果不指定会按照这个顺序查找配置文件：$CWD/supervisord.conf, $CWD/etc/supervisord.conf, /etc/supervisord.conf）： 我们把上面这部分配置保存到 /etc/supervisord.conf（或其他任意有权限访问的文件），然后启动 supervisord（通过 -c 选项指定配置文件路径，如果不指定会按照这个顺序查找配置文件：$CWD/supervisord.conf, $CWD/etc/supervisord.conf, /etc/supervisord.conf）：1supervisord -c /etc/supervisord.conf 查看 supervisord 是否在运行：1ps aux | grep supervisord program 配置上面我们已经把 supervisrod 运行起来了，现在可以添加我们要管理的进程的配置文件。这些配置可以都写到 supervisord.conf 文件里，如果应用程序很多，最好通过 include 的方式把不同的程序（组）写到不同的配置文件里。 为了举例，我们新建一个目录 /etc/supervisor/ 用于存放这些配置文件，相应的，把 /etc/supervisord.conf 里 include 部分的的配置修改一下：12[include]files = /etc/supervisor/*.conf 假设有个用 Flask 开发的用户系统 usercenter, 生产环境使用 gunicorn 运行。项目代码位于 /home/leon/projects/usercenter，WSGI 对象位于 wsgi.py。在命令行启动的方式是这样的：12cd /home/leon/projects/usercentergunicorn -w 8 -b 0.0.0.0:17510 wsgi:app 对应的配置文件可能是：12345678910111213[program:usercenter]directory = /home/leon/projects/usercenter ; 程序的启动目录command = gunicorn -w 8 -b 0.0.0.0:17510 wsgi:app ; 启动命令autostart = true ; 在 supervisord 启动的时候也自动启动startsecs = 5 ; 启动 5 秒后没有异常退出，就当作已经正常启动了autorestart = true ; 程序异常退出后自动重启startretries = 3 ; 启动失败自动重试次数，默认是 3user = leon ; 用哪个用户启动redirect_stderr = true ; 把 stderr 重定向到 stdout，默认 falsestdout_logfile_maxbytes = 20MB ; stdout 日志文件大小，默认 50MBstdout_logfile_backups = 20 ; stdout 日志文件备份数; stdout 日志文件，需要注意当指定目录不存在时无法正常启动，所以需要手动创建目录（supervisord 会自动创建日志文件）stdout_logfile = /data/logs/usercenter_stdout.log 其中 [program:usercenter] 中的 usercenter 是应用程序的唯一标识，不能重复。对该程序的所有操作（start, restart 等）都通过名字来实现。 Tips 1: Python 环境有两种方式指定程序使用的 Python 环境： command 使用绝对路径。假设使用 pyenv 来管理 Python 环境，上面例子中的 gunicorn 路径可以替换为/home/leon/.pyenv/versions/usercenter/bin/gunicorn. 这种方式一目了然，推荐。 通过 environment 配置 PYTHONPATH. environment=PYTHONPATH=$PYTHONPATH:/home/leon/.pyenv/versions/usercenter/bin/. environment 这个配置项非常有用，可以用来给程序传入环境变量。 Tips 2: 后台进程Supervisor 只能管理在前台运行的程序，所以如果应用程序有后台运行的选项，需要关闭。 Tips 3: 子进程有时候用 Supervisor 托管的程序还会有子进程（如 Tornado），如果只杀死主进程，子进程就可能变成孤儿进程。通过这两项配置来确保所有子进程都能正确停止：12stopasgroup=truekillasgroup=true 使用 supervisorctlSupervisorctl 是 supervisord 的一个命令行客户端工具，启动时需要指定与 supervisord 使用同一份配置文件，否则与 supervisord 一样按照顺序查找配置文件。1supervisorctl -c /etc/supervisord.conf 上面这个命令会进入 supervisorctl 的 shell 界面，然后可以执行不同的命令了：123456&gt; status # 查看程序状态&gt; stop usercenter # 关闭 usercenter 程序&gt; start usercenter # 启动 usercenter 程序&gt; restart usercenter # 重启 usercenter 程序&gt; reread ＃ 读取有更新（增加）的配置文件，不会启动新添加的程序&gt; update ＃ 重启配置文件修改过的程序 上面这些命令都有相应的输出，除了进入 supervisorctl 的 shell 界面，也可以直接在 bash 终端运行：123456$ supervisorctl status$ supervisorctl stop usercenter$ supervisorctl start usercenter$ supervisorctl restart usercenter$ supervisorctl reread$ supervisorctl update 使用web可视化管理除了 supervisorctl 之外，还可以配置 supervisrod 启动 web 管理界面，这个 web 后台使用 Basic Auth 的方式进行身份认证。1234[inet_http_server] ; HTTP 服务器，提供 web 管理界面port=0.0.0.0:9001 ; Web 管理后台运行的 IP 和端口，如果开放到公网，需要注意安全性username=user ; 登录管理后台的用户名password=123 ; 登录管理后台的密码 更改主配置文件的[inet_http_server]后重新运行,通过web端访问9001端口输入账号密码后会进入如下界面:可通过此界面对进程进行终止重启等操作 进程组当你要管理的子进程非常多时，Supervisor允许你将子进程分组，也就是多个子进程可以在同一组内统一管理。你只需要配置”[group:thegroupname]”块就行了。 我们可以试下，配置一个”[program:hello]”子程序，和”[program:world]”,然后加上”[group:xxx]”配置：12345678[program:hello]command=python hello.py[program:world]command=python world.py[group:helloworld]programs=hello,world 现在我们将子进程”hello”和”world”都配置在进程组”helloworld”中，启动supervisord，让我们在supervisorctl客户端下试一试：12345678910supervisorctl -c /etc/supervisord.confsupervisor&gt; status helloworld:*helloworld:hello RUNNING pid 75042, uptime 0:03:12helloworld:world RUNNING pid 75043, uptime 0:03:12supervisor&gt; stop helloworld:*helloworld:hello: stoppedhelloworld:world: stoppedsupervisor&gt; status helloworld:*helloworld:hello STOPPED Nov 27 11:05 PMhelloworld:world STOPPED Nov 27 11:05 PM 子进程名前都加了前缀”helloworld:”，这样我们使用通配符”appgroup:*”来执行的命令就对进程组内所有的子进程起效了. 开机自启动ubuntu系统编辑/etc/rc.local文件1vi /etc/rc.local 在exit 0 之前加入以下命令1/usr/local/bin/supervisord 保存并退出。最后修改rc.local权限1chmod +x /etc/rc.local centos系统新建文件supervisord.service12345678910111213141516#supervisord.service[Unit]Description=Supervisor daemon[Service]Type=forkingExecStart=/usr/bin/supervisord -c /etc/supervisord.confExecStop=/usr/bin/supervisorctl shutdownExecReload=/usr/bin/supervisorctl reloadKillMode=processRestart=on-failureRestartSec=42s[Install]WantedBy=multi-user.target 将文件拷贝到/usr/lib/systemd/system/1cp supervisord.service /usr/lib/systemd/system/ 启动服务1systemctl enable supervisord 验证一下是否为开机启动1systemctl is-enabled supervisord MacOs系统我们可以通过编写launched配置文件让supervisor开机自启，我们需要编辑/Library/LaunchDaemons/com.agendaless.supervisord.plist文件（需要root权限，如sudo）：12345678910111213141516171819202122&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;!DOCTYPE plist PUBLIC &quot;-//Apple//DTD PLIST 1.0//EN&quot; &quot;http://www.apple.com/DTDs/PropertyList-1.0.dtd&quot;&gt;&lt;plist version=&quot;1.0&quot;&gt;&lt;dict&gt; &lt;key&gt;KeepAlive&lt;/key&gt; &lt;dict&gt; &lt;key&gt;SuccessfulExit&lt;/key&gt; &lt;false/&gt; &lt;/dict&gt; &lt;key&gt;Label&lt;/key&gt; &lt;string&gt;com.agendaless.supervisord&lt;/string&gt; &lt;key&gt;ProgramArguments&lt;/key&gt; &lt;array&gt; &lt;string&gt;/usr/local/bin/supervisord&lt;/string&gt; &lt;string&gt;-n&lt;/string&gt; &lt;string&gt;-c&lt;/string&gt; &lt;string&gt;/etc/supervisord.conf&lt;/string&gt; &lt;/array&gt; &lt;key&gt;RunAtLoad&lt;/key&gt; &lt;true/&gt;&lt;/dict&gt;&lt;/plist&gt; 其中配置文件的位置可以根据情况改变（例子里是/etc/supervisord.conf）接着我们就可以注册这个plist了：1launchctl load /Library/LaunchDaemons/com.agendaless.supervisord.plist 如果一切正常，那么我们运行supervisorctl就可以看到正确的结果了：12&gt; sudo supervisorctlsupervisor&gt; 遇到的问题11unix:///tmp/supervisor.sock no such file 解决方案如下:1、打开配置文件1vim /etc/supervisord.conf 这里把所有的/tmp路径改掉，/tmp/supervisor.sock 改成 /var/run/supervisor.sock，/tmp/supervisord.log 改成 /var/log/supervisor.log，/tmp/supervisord.pid 改成 /var/run/supervisor.pid 要不容易被linux自动清掉2、修改权限12sudo chmod 777 /runsudo chmod 777 /var/log 3、创建supervisor.sock12sudo touch /var/run/supervisor.socksudo chmod 777 /var/run/supervisor.sock 4、启动supervisord(可能需要等待短暂的时间过后才可以成功启动)注意stop之前的实例或杀死进程 遇到的问题21Error: Another program is already listening on a port that one of our HTTP servers is configured to use. Shut this program down first before starting supervisord. 解决方案:123sudo unlink /tmp/supervisor.socksudo unlink /var/run/supervisor.sock Supervisor 有很丰富的功能，还有其他很多项配置，可以在官方文档获取更多信息：http://supervisord.org/index.html 本文参考自liyangliang.me,并对其文章进行补充~]]></content>
      <categories>
        <category>技术相关</category>
        <category>python</category>
      </categories>
      <tags>
        <tag>进程管理</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[3proxy代理服务器使用]]></title>
    <url>%2Fpost%2F942b36a3.html</url>
    <content type="text"><![CDATA[3proxy 简介3proxy是一个代理服务器,功能十分强大,可以同时开启http、socks等各种代理,支持 windows 和 linux 平台，安装和配置都很简单,使用非常方便. 配置安装3proxyLINUX 系统安装步骤如下：12345678# 通过 git 下载最新版 3proxy 源码git clone https://github.com/z3APA3A/3proxy.git# 切换到 3proxy 目录cd 3proxy# 编译 3proxy, 如果你的系统没有 gcc，你需求先安装 gccmake -f Makefile.Linux# 安装 3proxymake -f Makefile.Linux install 编辑配置文件3proxy.cfg,如果没有该文件可以自行创建 1234567891011121314151617# dns服务器第一个解析失败会使用第二个nserver 223.5.5.5 nserver 114.114.114.114# 域名解析最大的缓存条目 默认即可，也可以注释掉nscache 65536# 各项连接选项的超时时间# timeouts &lt;BYTE_SHORT&gt; &lt;BYTE_LONG&gt; &lt;STRING_SHORT&gt; &lt;STRING_LONG&gt; &lt;CONNECTION_SHORT&gt; &lt;CONNECTION_LONG&gt; &lt;DNS&gt; &lt;CHAIN&gt;timeouts 1 5 30 60 180 1800 15 60# 用户名密码 如果auth为strong情况需要验证users zds:CL:a95j1JB3Txvq71D20G88oi# auto none 无需验证auth strong # 开启验证flush# 开启一个HTTP代理 端口是9000proxy -a -i0.0.0.0 -p9000# 开启一个SOCKS代理 端口是9001socks -a -i0.0.0.0 -p9001 后台执行13proxy 3proxy.cfg&amp; windows 系统安装步骤如下:首先点击下载应用程序安装好之后执行命令:1234# 3proxy.cfg路径更改为自己的3proxy.cfg3proxy --install c:\service\3proxy\bin\3proxy.cfgnet start 3proxy # 启动服务 可以通过以下代码在本地来检查是否可以正常使用:12345678910111213import requestsfrom requests.auth import HTTPProxyAuth# 验证auth = HTTPProxyAuth('zds', 'a95j1JB3Txvq71D20G88oi') # cfg配置文件里设置的账号密码url = 'http://ip.6655.com/ip.aspx'proxies = &#123; 'http': 'http://**.**.**.**:9000' # 服务器外网ip&#125;r = requests.get(url, proxies=proxies, auth=auth)print(r.text) 如果正确输出你的服务器ip表示开启成功!]]></content>
      <categories>
        <category>代理</category>
        <category>服务器</category>
      </categories>
      <tags>
        <tag>代理</tag>
        <tag>服务器</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[python3 线程 进程 异步io性能对比]]></title>
    <url>%2Fpost%2F51704.html</url>
    <content type="text"><![CDATA[&emsp;&emsp;目前网络上主流爬虫一般在使用requests、多线程、多进程,却很少有人使用协程异步这种 速度快占用资源少的爬取方式,当然爬虫的关键往往并不在于爬取速度的快慢,每种方式也有它各 自的适用场景,这里我们仅对网络io密集型进行简要的性能对比。 1.多线程爬虫&emsp;&emsp;众所周知由于python中一个线程需要执行任务，必须获取GIL,但是，在I/O阻塞的时候，解 释器会释放GIL。所以GIL在io密集型任务里仍有优异的性能表现,这里我们均以请求 wangfan.me 1000次为例进行测试,代码如下: 12345678910111213141516171819202122232425262728import requestsfrom threading import Threadimport timeurl = 'http://wangfan.me'headers = &#123; 'User-Agent': 'Mozilla/5.0 (iPhone; CPU iPhone OS 11_0 like Mac OS X) AppleWebKit/604.1.38 (KHTML, like Gecko) Version/11.0 Mobile/15A372 Safari/604.1'&#125;def request_url(): for _ in range(2): r = requests.get(url, headers=headers) print(r.status_code)def main(): s = time.time() threads = [] for _ in range(500): threads.append(Thread(target=request_url)) for t in threads: t.start() for t in threads: t.join() print('执行总用时 &#123;&#125;秒'.format(time.time() - s))if __name__ == '__main__': main() 程序执行结果: 用时约12.6s! 2.多进程爬虫&emsp;&emsp;由于进程之间的通信有操作系统传递，导致通讯效率低，切换开销大。所以一般密集CPU 任务，需要充分使用多核CPU资源（服务器，大量的并行计算）的时候用多进程。在编写爬虫 时不建议单独使用多进程来编写,代码如下: 12345678910111213141516171819202122232425262728import requestsfrom multiprocessing import Processimport timeurl = 'http://wangfan.me'headers = &#123; 'User-Agent': 'Mozilla/5.0 (iPhone; CPU iPhone OS 11_0 like Mac OS X) AppleWebKit/604.1.38 (KHTML, like Gecko) Version/11.0 Mobile/15A372 Safari/604.1'&#125;def request_url(): for _ in range(2): r = requests.get(url, headers=headers) print(r.status_code)def main(): s = time.time() threads = [] for _ in range(500): threads.append(Process(target=request_url)) for t in threads: t.start() for t in threads: t.join() print('执行总用时 &#123;&#125;秒'.format(time.time() - s))if __name__ == '__main__': main() 程序执行结果: 用时约17.7s! 3.异步爬虫&emsp;&emsp;async 与 await， 这是在 Python3.5 中引入的新语法，用于增强 Python 协程方面的支持。 可以简单的认为是asyncio.coroutine与yield from的简化版。随着新语法到来的还有async with EXPR as VAR这个异步上下文管理器（取代with (yield from EXPR) as VAR)与async for TARGET in ITER(取代for TARGET in (yield from ITER))这个异步迭代器。不同于yield from，await只适用 于 CO_COROUTINE标记的原生协程，即，使用async def语法定义的对象。由于requests并不支 持异步请求,所以请求库我们选择使用aiohttp(pip install aiohttp),参考代码如下: 1234567891011121314151617181920import timeimport asyncioimport aiohttpurl = 'http://wangfan.me'async def _get_page(): """ 获取并返回网页内容 """ async with aiohttp.request('GET', url) as session: async with session as s: print(s.status)if __name__ == '__main__': s = time.time() a = [_get_page() for _ in range(20)] loop = asyncio.get_event_loop() loop.run_until_complete((asyncio.gather(*a))) print('执行总用时 &#123;&#125;'.format(time.time() - s)) 程序执行结果: 用时约7.7秒! 测试结果 &emsp;&emsp;根据测试结果不难看出,关于io密集型任务,异步&gt;多线程&gt;多进程,由于请求时可能受网络状况的 影响,所以运行时间的结果,是经过多次运行选取了较为合理的中位值,这里异步爬虫仅用时7.7秒,最快 时能达到5秒左右是测试前完全没有想到的,甚至在得到结果时一度以为aiohttp有缓存功能,但是当我 将url更改为某网站翻页url,每次请求不同url时仍能保持该速度!!!多进程在执行时cpu完全跑满,导致本 机操作卡顿,所以无论就速度而言还是占用资源来讲都不建议使用多进程来编写爬虫,线程任务选择了 500线程每个线程请求两次速度上还是与异步有一定差距,所以如果要编写对爬取速度要求较高的代 码时可以优先考虑aiohttp与asyncio的异步编程!]]></content>
      <categories>
        <category>python</category>
        <category>爬虫</category>
      </categories>
      <tags>
        <tag>多线程</tag>
        <tag>多进程</tag>
        <tag>协程</tag>
        <tag>性能优化</tag>
        <tag>异步</tag>
        <tag>爬虫</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[绑定gihub page到Godaddy域名]]></title>
    <url>%2Fpost%2F55704.html</url>
    <content type="text"><![CDATA[一、首先在Godaddy官网购买域名二、使用dnspod配置DNS首先，进入DNSPOD官方网站，用你的账号登录。登录后，进入DNSPOD控制面板，选择“我的域名”模块。在“我的域名”页面，有一个【添加域名】按钮。如图片一所示。点击该按钮，这时比如我要解析hgaga.com ，直接输入你想解析的域名进去。（注意，输入域名时，不要带www）。如图片所示。点击【确定】，这样就添加了一个域名到DNSPOD了。添加好之后，需要为域名设置各种参数，如A记录，NS地址等。点击刚才添加的这个域名，进入域名设置界面。在这里，你可以看到DNSPOD已经为你提供了2个NS（Name Server）地址，你需要把这2个地址，在GoDaddy后台控制面板中进行设置如图片所示更改完域名服务器后回到dnspod的管理界面,添加一下两条记录alla.ns.cloudflare.com evan.ns.cloudflare.com| 主机记录 | 记录类型 | 记录值 || ——— | — |– || www | CNAME| username.github.io|| @ | A| 185.199.111.153 || @ | A| 185.199.110.153 | 这样别人用www和不用www都能访问你的网站（其实www的方式，会先解析成http://xxxx.github.io， 然后根据CNAME再变成http://xxx.com， 即中间是经过一次转换的）。 三、配置Github在我们的Hexo项目的sources目录下新建个CNAME文件，里面写上我们的域名。之后重新部署项目: hexo g hexo d 如果你是用 hexo 框架搭建博客并部署到 Github Pages 上: 每次hexo g hexo d 后会把你的博客所在目录下 public 文件夹里的东西都推到 Github Pages 仓库上，并且把 CNAME 文件覆盖掉，解决这个问题可以直接把 CNAME 文件添加到 source 文件夹里，这样每次推的时候就不用担心仓库里的 CNAME 文件被覆盖掉了。 开启HTTPS点击setting填写custom domain并勾选enforce https 全部设置完成后,需要等待10分钟左右域名才会生效.]]></content>
      <categories>
        <category>技术相关</category>
      </categories>
      <tags>
        <tag>域名绑定</tag>
        <tag>godaddy</tag>
      </tags>
  </entry>
</search>
